# initialize the package for local modules with pip install -e .

from setuptools import setup, find_packages

setup(
    name='streamy-bot',
    version='0.1.0',
    description='A bot to stream your music tracks. Pump those numbers!',
    author='I.M. Wright',
)
